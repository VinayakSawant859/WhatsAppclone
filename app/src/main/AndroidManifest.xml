<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.whatsappclone">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_whatsapp_clone_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_whatsapp_clone_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppClone"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".Activities.ProfileSetupActivity"
            android:exported="false" />
        <activity
            android:name=".Activities.OTPActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".Activities.PhoneVerificationActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>